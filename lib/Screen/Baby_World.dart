import 'package:flutter/material.dart';
class BabyWorld extends StatelessWidget {
  const BabyWorld ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Baby World')),
    );
  }
}