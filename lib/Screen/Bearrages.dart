import 'package:flutter/material.dart';
class Bearrages extends StatelessWidget {
  const Bearrages ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Baerrages')),
    );
  }
}